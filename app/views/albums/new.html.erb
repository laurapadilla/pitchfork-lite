<h1>Add an album</h1>

<%= simple_form_for [@category, @album] do |f| %>
<%= f.input :title %>
<%= f.input :artist %>
<%= f.input :release %>
<%= f.input :body %>

<%= f.button :submit, "Add album" %>
<% end %>